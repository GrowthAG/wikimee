<nav class="fixed top-0 left-0 right-0 z-50 flex items-center h-[4.5rem] px-[1.5rem] bg-[#0f0d1a]/80 backdrop-blur-md border-b border-white/5 transition-all duration-300">
  <div class="w-full max-w-[80rem] mx-auto flex items-center justify-between">
    <!-- Logo -->
    <a href="/" class="flex items-center gap-[0.75rem]" aria-label="Wikimee Home">
      <img src="/logo.png" alt="Wikimee" class="h-[2rem] w-auto object-contain brightness-0 invert" />
    </a>

    <!-- Menu Actions -->
    <div class="hidden md:flex items-center gap-[1.5rem]">
      <a href="#features" class="text-[0.875rem] font-medium text-white/70 hover:text-white transition-colors">Funcionalidades</a>
      <a href="#pricing" class="text-[0.875rem] font-medium text-white/70 hover:text-white transition-colors">Preços</a>
    </div>

    <!-- CTA -->
     <button 
      onclick="openModal()"
      class="px-[1.25rem] py-[0.5rem] bg-white/10 hover:bg-white/20 border border-white/10 text-white text-[0.875rem] font-medium rounded-full transition-all duration-200"
    >
      Agendar Demo
    </button>
  </div>
</nav>
